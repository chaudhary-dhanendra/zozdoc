# Crate Dependency Example

`hermes-matching` may depend on `hermes-book`, `hermes-domain`, `hermes-events`, `hermes-fixed`, and `hermes-ids`. It must not depend on `hermes-gateway`, database clients, Kafka clients, or HTTP clients.
